get:
  summary: Get all available rooms (Public – supports filters)
  tags: [Rooms]
  parameters:
    - name: city
      in: query
      schema: { type: string }
    - name: minCapacity
      in: query
      schema: { type: integer }
    - name: priceMax
      in: query
      schema: { type: number }
  responses:
    '200':
      description: List of rooms
      content:
        application/json:
          schema:
            type: array
            items: { $ref: ../components/schemas/RoomPublic.yaml }