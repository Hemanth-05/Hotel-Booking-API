get:
  tags: [Rooms]
  summary: Get all rooms (public)
  responses:
    200:
      description: Room list
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../components/schemas/Room.yaml
