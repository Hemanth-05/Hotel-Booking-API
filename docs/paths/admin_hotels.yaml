get:
  summary: Get all hotels (Admin only)
  tags: [Hotels]
  security: [bearerAuth: []]
  responses:
    '200':
      description: List of all hotels
      content:
        application/json:
          schema:
            type: array
            items: { $ref: ../components/schemas/HotelResponse.yaml }
    '403':
      $ref: ../components/responses/403.yaml