Forbidden:
  description: "Forbidden – user does not have permission"
  content:
    application/json:
      schema:
        type: object
        properties:
          message:
            type: string
            example: "Access denied"
